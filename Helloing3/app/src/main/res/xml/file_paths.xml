<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pour les images de la caméra -->
    <external-files-path
        name="images"
        path="Pictures/" />

    <!-- Pour tous les fichiers dans le stockage externe (galerie, téléchargements, etc.) -->
    <external-path
        name="external_media"
        path="." />  <!-- Permet l'accès à tous les fichiers dans le stockage externe -->
</paths>
